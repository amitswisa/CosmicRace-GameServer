package utils.messages_manager;

import utils.GlobalSettings;

public class HandleClientMessage {

    public static void ProcessMessage(String i_Message){
        if(i_Message.toUpperCase().equals(GlobalSettings.CLIENT_QUITED_BY_CHOICE)){

        }else { //probably json.

        }
    }
}
